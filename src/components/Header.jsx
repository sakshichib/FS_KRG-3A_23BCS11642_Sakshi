

const Header = ({ title }) => (
    <header style={{padding: "0.5 rem", backgroundColor: "green", color: "white", textAlign: "center"}}> 
    <h1>{title}</h1>
    </header>
);


export default Header;